{
  "name": "weather-simulation",
  "version": "0.1.0",
  "license": "MIT",
  "repository": "",
  "scripts": {
    "ls": "ls -la",
    "push-serve": "./node_modules/.bin/https-pushstate-server -r . -p 9876 -s -c ./certs/cert.pem -k ./certs/key.pem -d data,styles,images,scripts"
  },
  "pre-commit": [],
  "private": true,
  "engines": {
    "node": ">= 6.9.2",
    "npm": ">= 3.10.9"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^6.1.2",
    "eslint": "^3.19.0",
    "https-pushstate-server": "git+ssh://git@github.com/aerofs/https-pushstate-server.git"
  }
}
